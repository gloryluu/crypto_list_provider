import 'package:flutter/material.dart';

class CryptoDetailView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crypto [name]'),
      ),
      body: Center(child: Text('TODO: CryptoDetailView')),
    );
  }
}